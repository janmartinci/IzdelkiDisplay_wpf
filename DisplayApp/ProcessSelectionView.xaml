<Page x:Class="DisplayApp.ProcessSelectionView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      xmlns:local="clr-namespace:DisplayApp"
      mc:Ignorable="d" 
      d:DesignHeight="500" d:DesignWidth="1000"
      Title="ProcessSelectionView">
    <Grid>
        <materialDesign:Card Background="#F6F5F8" UniformCornerRadius="0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid Grid.Column="0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Button Grid.Row="0" Background="Transparent" BorderThickness="0" Padding="0" Cursor="Hand" HorizontalAlignment="Right" Margin="0 0 40 30" VerticalAlignment="Bottom" Click="Button_Click_Steps">
                        <materialDesign:Card Background="#FF3A3A3A" Width="250" Height="100" UniformCornerRadius="10">
                            <Button Background="Transparent" BorderThickness="0" Padding="0" Cursor="Hand">
                                <materialDesign:Card Background="#FF3A3A3A" Width="250" Height="100" UniformCornerRadius="10">
                                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0 0">
                                        <materialDesign:PackIcon Foreground="Aqua" Kind="Tag" Width="50" Height="50" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0 0"/>
                                        <TextBlock Text="Diaprojekcija izdelkov" Foreground="White" FontWeight="Bold" FontSize="16" Margin="3" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </materialDesign:Card>
                            </Button>
                        </materialDesign:Card>
                    </Button>
                    <Button Grid.Row="1" Background="Transparent" BorderThickness="0" Padding="0" Cursor="Hand" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0 30 40 0" Click="Button_Click_Custom">
                        <materialDesign:Card Background="#FF3A3A3A" Width="250" Height="100" UniformCornerRadius="10">
                            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0 0">
                                <materialDesign:PackIcon Foreground="Aqua" Kind="Form" Width="50" Height="50" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0 0"/>
                                <TextBlock Text="Diaprojekcija izdelkov" Foreground="White" FontWeight="Bold" FontSize="16" Margin="0 3 0 0" HorizontalAlignment="Center" VerticalAlignment="Bottom"/>
                                <TextBlock Text="(Po meri)" Foreground="White" FontWeight="Bold" FontSize="16" Margin="0" HorizontalAlignment="Center" VerticalAlignment="Bottom"/>
                            </StackPanel>
                        </materialDesign:Card>
                    </Button>
                </Grid>
                <Grid Grid.Column="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Button Grid.Row="0" Background="Transparent" BorderThickness="0" Padding="0" Cursor="Hand" HorizontalAlignment="left" VerticalAlignment="Bottom" Margin="40 0 0 30" Click="oglas_click">
                        <materialDesign:Card Background="#FF3A3A3A" Width="250" Height="100" UniformCornerRadius="10">
                            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0 0">
                                <materialDesign:PackIcon Foreground="Aqua" Kind="Advertisements" Width="50" Height="50" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0 0"/>
                                <TextBlock Text="Oglas" Foreground="White" FontWeight="Bold" FontSize="16" Margin="3" HorizontalAlignment="Center" VerticalAlignment="Bottom"/>
                            </StackPanel>
                        </materialDesign:Card>
                    </Button>
                    <Button Grid.Row="1" Background="Transparent" BorderThickness="0" Padding="0" Cursor="Hand" HorizontalAlignment="left" VerticalAlignment="top" Margin="40 30 0 0" Click="video_predvajalnik_click">
                        <materialDesign:Card Background="#FF3A3A3A" Width="250" Height="100" UniformCornerRadius="10">
                            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0 0">
                                <materialDesign:PackIcon Foreground="Aqua" Kind="Video" Width="50" Height="50" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0 0"/>
                                <TextBlock Text="Video predvajalnik" Foreground="White" FontWeight="Bold" FontSize="16" Margin="3" HorizontalAlignment="Center" VerticalAlignment="Bottom"/>
                            </StackPanel>
                        </materialDesign:Card>
                    </Button>
                </Grid>
            </Grid>
        </materialDesign:Card>
    </Grid>
</Page>
