<Page x:Class="DisplayApp.ProcessSelectionView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      xmlns:local="clr-namespace:DisplayApp"
      mc:Ignorable="d" 
      d:DesignHeight="500" d:DesignWidth="1000"
      Title="ProcessSelectionView">
    <Grid>
        <materialDesign:Card Background="#F6F5F8" UniformCornerRadius="15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid Grid.Column="0">
                    <materialDesign:Card Background="#FF3A3A3A" Width="250" Height="300" UniformCornerRadius="10">
                        <Button Background="Transparent" BorderThickness="0" Padding="0" Cursor="Hand">
                            <materialDesign:Card Background="#FF3A3A3A" Width="250" Height="300" UniformCornerRadius="10">
                                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                    <TextBlock Text="Preko izbire znamke" Foreground="White" FontWeight="Bold" FontSize="16" Margin="10" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </StackPanel>
                            </materialDesign:Card>
                        </Button>
                    </materialDesign:Card>
                </Grid>
                <Grid Grid.Column="1">
                    <Button Background="Transparent" BorderThickness="0" Padding="0" Cursor="Hand">
                        <materialDesign:Card Background="#FF3A3A3A" Width="250" Height="300" UniformCornerRadius="10">
                            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                <TextBlock Text="Posamezni artikel" Foreground="White" FontWeight="Bold" FontSize="16" Margin="10" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </StackPanel>
                        </materialDesign:Card> 
                    </Button>
                </Grid>
            </Grid>
        </materialDesign:Card>
    </Grid>
</Page>
