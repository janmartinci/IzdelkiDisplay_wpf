<Page x:Class="DisplayApp.ProcessSelectionView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      xmlns:local="clr-namespace:DisplayApp"
      mc:Ignorable="d" 
      d:DesignHeight="500" d:DesignWidth="1000"
      Title="ProcessSelectionView">
    <Grid>
        <materialDesign:Card Background="#F6F5F8" UniformCornerRadius="15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid Grid.Column="0">
                    <Button Background="Transparent" BorderThickness="0" Padding="0" Cursor="Hand" HorizontalAlignment="Right" Margin="0 0 50 0" VerticalAlignment="Center" Click="Button_Click_Steps">
                        <materialDesign:Card Background="#FF3A3A3A" Width="250" Height="300" UniformCornerRadius="10">
                            <Button Background="Transparent" BorderThickness="0" Padding="0" Cursor="Hand">
                                <materialDesign:Card Background="#FF3A3A3A" Width="250" Height="300" UniformCornerRadius="10">
                                    <StackPanel VerticalAlignment="Bottom" HorizontalAlignment="Center" Margin="0 60">
                                        <materialDesign:PackIcon Foreground="Aqua" Kind="Tag" Width="100" Height="100" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0 20"/>
                                        <TextBlock Text="Voden vnos" Foreground="White" FontWeight="Bold" FontSize="16" Margin="10" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </materialDesign:Card>
                            </Button>
                        </materialDesign:Card>
                    </Button>
                </Grid>
                <Grid Grid.Column="1">
                    <Button Background="Transparent" BorderThickness="0" Padding="0" Cursor="Hand" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="50 0 0 0" Click="Button_Click_Custom">
                        <materialDesign:Card Background="#FF3A3A3A" Width="250" Height="300" UniformCornerRadius="10">
                            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0 60">
                                <materialDesign:PackIcon Foreground="Aqua" Kind="Form" Width="100" Height="100" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0 20"/>
                                <TextBlock Text="Prilagojen vnos" Foreground="White" FontWeight="Bold" FontSize="16" Margin="10" HorizontalAlignment="Center" VerticalAlignment="Bottom"/>
                            </StackPanel>
                        </materialDesign:Card> 
                    </Button>
                </Grid>
            </Grid>
        </materialDesign:Card>
    </Grid>
</Page>
